/**
 * Browser Constants
 *
 * Constants used by the browser service and related utilities.
 */

/**
 * Data attribute used to track event listeners on DOM elements.
 * Event listeners attached via addEventListener or inline handlers
 * will be recorded in this attribute as a JSON array.
 */
export const INTERACTIVE_DATA_ATTRIBUTE = "data-vibe-testing-flags";
